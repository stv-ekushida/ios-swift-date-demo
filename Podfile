# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ios-swift-date-demo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ios-swift-date-demo
  pod 'SwiftDate', '~> 4.0'

  target 'ios-swift-date-demoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ios-swift-date-demoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
